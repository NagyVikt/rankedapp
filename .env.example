# .env.example - Környezeti változók sablonja
# Másold át ezt a fájlt .env.local néven és töltsd ki a saját értékeiddel.
# Soha ne tedd a .env.local fájlt a verziókövető rendszerbe!

# Next.js / Vercel specifikus
NEXTAUTH_URL=http://localhost:3000
# Generálj egy erős titkos kulcsot (pl. `openssl rand -base64 32` paranccsal)
NEXTAUTH_SECRET=
# Vercel Blob Storage token (Vercel dashboardról)
BLOB_READ_WRITE_TOKEN=
# Általában "false", fejlesztéshez lehet "true"
BYPASS_JSON_VALIDATION=false
# Maximális futási idő másodpercben szerver nélküli funkciókhoz
MAX_DURATION=60
# Alap URL a szerver oldali műveletekhez (pl. webhookok)
BASE_URL=http://localhost:3000

# Supabase (Backend as a Service)
# A Supabase projekt URL-je (Supabase dashboard -> Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
# A Supabase projekt nyilvános (anon) kulcsa (Supabase dashboard -> Project Settings -> API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# A Supabase projekt service role kulcsa (Óvatosan használd! Supabase dashboard -> Project Settings -> API)
SUPABASE_SERVICE_ROLE_KEY=
# Adatbázis kapcsolati string (Supabase dashboard -> Project Settings -> Database -> Connection pooling)
# Használd a Pooler connection stringet Drizzle-hez
DATABASE_URL=
# Alternatív/régi Supabase adatbázis URL (ha szükséges)
POSTGRES_URL=

# AI Szolgáltatók API Kulcsai
# OpenAI API kulcs (OpenAI platform)
OPENAI_API_KEY=
# OpenRouter API kulcs (OpenRouter platform)
OPENROUTER_API_KEY=
# Together AI API kulcs (Together AI platform)
TOGETHER_API_KEY=
# Firecrawl API kulcs (Firecrawl platform) web tartalom lekéréséhez
FIRECRAWL_API_KEY=
# Google Gemini API kulcs (Google AI Studio) - Ha használod
# GOOGLE_API_KEY=
# DeepSeek API kulcs (DeepSeek platform) - Ha használod
# DEEPSEEK_API_KEY=

# Stripe (Fizetési szolgáltató)
# Stripe titkos kulcs (Stripe dashboard -> Developers -> API keys)
STRIPE_SECRET_KEY=
# Stripe webhook titkos kulcs (Stripe dashboard -> Developers -> Webhooks -> Endpoint secret)
STRIPE_WEBHOOK_SECRET=

# Upstash (Redis / Rate Limiting)
# Upstash Redis REST URL (Upstash console)
UPSTASH_REDIS_REST_URL=
# Upstash Redis REST Token (Upstash console)
UPSTASH_REDIS_REST_TOKEN=

# AuthJS (NextAuth v5) - Opcionális Social Loginok
# Generálj egy erős titkos kulcsot (pl. `openssl rand -base64 32` paranccsal)
AUTH_SECRET=
# Facebook App ID és Secret (Meta for Developers)
AUTH_FACEBOOK_ID=
AUTH_FACEBOOK_SECRET=
# GitHub App Client ID és Secret (GitHub Developer settings)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
# Google Cloud Client ID és Secret (Google Cloud Console -> APIs & Services -> Credentials)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Twitter (X) App API Key és Secret (Twitter Developer Portal)
AUTH_TWITTER_ID=
AUTH_TWITTER_SECRET=




